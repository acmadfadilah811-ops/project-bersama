# Daily Log 2026-07-31

- **T-629 (Codex)**: Tutup Buku sekarang menolak periode yang memiliki saldo
  abnormal negatif menurut arah normal akun (contoh Kas) tanpa mengubah jurnal
  atau mengunci periode. Ditambahkan endpoint detail jurnal posted per periode
  dan UI Tutup Buku Semua Bulan berbasis API nyata; Detail tampil layar penuh
  dengan tombol Kembali saja serta kolom tanggal, nomor transaksi, nomor/nama
  akun, deskripsi, debit, dan kredit. Test periode 6/6, migration check, lint,
  build frontend, dan Graphify update lulus. Status `review`.
- Klarifikasi scope: saldo Kas minus yang dilihat pengguna terjadi di Olsera,
  bukan di database Bintang ERP; tidak ada jurnal atau data Bintang yang diubah
  untuk kasus tersebut.
- **T-629 revisi UI (Codex)**: menu aktif `TutupBukuTokoIni` ternyata masih
  memakai UI statis lama. Diganti dengan daftar periode API yang rapi,
  status dipisah dari kolom Aksi, dan aksi sekarang eksplisit `Detail`.
  Drawer/pengaturan dummy serta opsi abaikan saldo minus dihapus. ESLint dan
  build lulus; status kembali `review`.
- **T-629 Pengaturan Tutup Buku (Claude, instruksi eksplisit user)**: dua
  pertanyaan terbuka Codex diputuskan & diimplementasi. (1) Jurnal penutup
  fisik bulanan DITOLAK — akan merusak agregasi Laba Rugi bulan yang sama
  (Neraca menghitung "Pendapatan periode ini" langsung dari jurnal per
  rentang tanggal, bukan dari jurnal penutup). (2) Sebagai gantinya: field baru
  `AccountingSettings.closing_account` (migration 0033) memberi label akun
  ekuitas nyata pada baris "Pendapatan periode ini" di Neraca tanpa posting
  jurnal baru (drilldown tetap diblok, `id` baris None); field lama
  `Account.ignore_minus_closing` (sudah ada, belum dipakai) kini disambungkan
  ke validasi tutup buku — akun yang ditandai dikecualikan dari blokir saldo
  negatif. UI: drawer "Pengaturan" baru di `TutupBukuTokoIni.jsx`. Test baru
  di `tests_balance_sheet.py`/`tests_period_close.py`; full suite
  `accounting api` 285/285 lulus. Status `review`.
- **T-629 Tutup Buku Semua Bulan (Claude, instruksi eksplisit user)**: header
  `TutupBukuTokoIni.jsx` diubah jadi satu tombol "Tutup Buku" dengan dropdown
  2 opsi — Bulan Ini (perilaku lama) dan Semua Bulan (baru). "Semua Bulan"
  menutup semua periode Terbuka yang sudah berakhir untuk tahun terpilih,
  urut dari tertua, satu per satu; kegagalan satu bulan (mis. saldo negatif)
  dilewati dan dilaporkan, tidak menghentikan bulan lain (`close_all_open_periods()`
  di `services/period.py`, endpoint baru `POST /api/accounting/periods/close-all/`).
  Modal hasil menampilkan jumlah berhasil/gagal + alasan per bulan gagal.
  4 test baru (`CloseAllOpenPeriodsTestCase`); full suite `accounting api`
  288/288 lulus, 0 regresi; tidak ada migration baru. Status tetap `review`.
- **T-630 Export/Import Excel & PDF Akuntansi (Claude, instruksi eksplisit
  user)**: audit 28 file → 10 titik tidak berfungsi (6 toast palsu: Neraca,
  Laba Rugi, Export Piutang/Hutang, Simpanan Pelanggan x2; 4 tombol "Cetak
  jurnal" mati tanpa `onClick`, 3 di antaranya malah `disabled` permanen).
  Semua diperbaiki: Neraca/Laba Rugi dapat endpoint export xlsx backend baru
  (`report_export.py`); Piutang/Hutang/Simpanan Pelanggan export client-side
  (`exportXlsx.js`, pakai `xlsx`/SheetJS yang sudah jadi dependency); 4 tombol
  Cetak jurnal → `window.print()`. Bonus fix: `ExportPiutangModal.jsx` ternyata
  melanggar Rules of Hooks (`return null` sebelum hooks) + melebihi limit JSX
  300 baris — dipecah jadi `ExportRowRangeSelect.jsx`/`ExportStatusButtons.jsx`.
  Ditemukan & dilaporkan (tidak dikerjakan, di luar scope): 4 file form jurnal
  sudah 700+ baris sejak sebelum disentuh, jauh di atas limit.
  Investigasi lanjutan atas laporan user (kolom Pelanggan/Supplier/Email kosong
  di Kas & Bank): akar masalah `pos_posting.py`/`order_posting.py` tidak pernah
  isi `JournalEntryLine.customer/.supplier` (Contact/Order tidak kompatibel
  tipe dengan model Customer/Supplier, sengaja dipisah). Fix: resolve nama dari
  transaksi asal via `source_type`/`source_id` (`_resolve_pelanggan_supplier()`
  baru di `services/ledger.py`) — sempat salah asumsi `source_id` = id dokumen
  utama, ternyata `OrderActivityLog.id`/`PurchasePayment.id`, dikoreksi setelah
  test gagal & baca kode posting langsung. Kolom Email baru ditambahkan ke
  serializer + export Buku Besar/Kas & Bank/Rincian Mutasi Akun. 9 test baru
  (`tests_ledger_running_balance.py`, termasuk parsing xlsx asli via openpyxl);
  full suite `accounting api` 297/297 lulus, 0 regresi, tidak ada migration
  baru. Status `review`.
- **Revisi UI Kasir v2 — aturan koneksi data asli (Claude manager)**: user minta
  arahan sebelum mulai perbaikan UI Kasir POS. Dicek langsung kode `PosTerminal.jsx`
  + `api/pos_settings.py`: pola sudah benar (baca `GET /pos/sales/pos-rules/`,
  bukan menafsirkan `pos_ext_settings` mentah sendiri di frontend) — wajib
  dipertahankan di v2. Temuan baru: 3 dari 5 toggle "Mode - cek stok"
  (`pos_stok_selalu_cek_sebelum_order`, `pos_stok_transfer_harus_proses_penerima`,
  `pos_stok_posting_otomatis_laba_rugi`) tersimpan tapi tidak pernah dibaca di
  mana pun (bukan tugas baru, dicatat sebagai peringatan). Checklist "selesai per
  layar" ditambahkan ke [[Revisi UI Kasir v2]] berdasar pola bug T-614/T-629/T-630
  minggu ini. T-401 (requirement dari user) masih jadi gerbang wajib sebelum
  implementasi T-403+ dimulai — belum diisi.
- **T-614 Penyesuaian Hak Akses — scope disederhanakan (Claude, instruksi
  eksplisit user)**: desain awal (matriks 89-checkbox granular per modul,
  butuh ~89 titik enforcement baru) ditolak user setelah dijelaskan risikonya.
  Ditemukan sistem hak-akses lain yang SUDAH nyata & berfungsi (bukan
  `PenyesuaianHakAkses.jsx` yang dummy): `utils/permissions.js` +
  `ProtectedRoute.jsx` + tab "Hak Akses" di Pengaturan Toko. Gap: rute
  `/accounting-internal` tidak pernah terdaftar di sana, jadi sama sekali
  tidak dijaga — siapa pun yang login bisa akses langsung via URL, dan Admin
  tidak melihat menunya meski backend (`IsOwnerOrManager`) sudah mengizinkan.
  Diperbaiki: daftarkan `accounting-internal` ke `MENU_FEATURES` +
  `DEFAULT_PERMISSIONS` (owner/manager/admin), tambah menu Admin di
  `Sidebar.jsx`. Backend accounting sudah aman (dicek ulang semua
  `permission_classes`). ESLint + build lulus. Status `review`.
