---
date: 2026-08-09
---

- T-716 `review`: bug ditemukan & diperbaiki oleh Claude (manager, instruksi
  eksplisit user) — staff tidak bisa claim/start/complete SPK yang diterbitkan
  kasir dari POS (`JobBoardViewSet` crash `AttributeError` karena akses
  `job.order_item.order` tanpa null-check). Log aktivitas order & pemotongan
  BoM otomatis kini di-skip untuk job POS (tidak ada Order induk); staff bisa
  proses job POS end-to-end. 2 test baru + full suite 400/400 lulus lokal.
  Dideploy ke VPS `38.253.224.44` (rebuild+restart container `backend` via
  Docker Compose, bukan `git pull`/systemd seperti asumsi script lama yang
  sudah stale — fix disalin langsung + backup dibuat, health check 200,
  dikonfirmasi ada di container live).
- T-717 `review`: migration `voided_at`/`voided_by` untuk `POSSale` ternyata
  sudah ada di disk lokal (dibuat agent lain) tapi tak pernah di-commit,
  sehingga tak pernah sampai ke VPS. Disalin ke VPS + diterapkan ke Postgres
  nyata (dikonfirmasi lewat `psql \d api_possale`), backend container
  di-rebuild & restart, health check 200. Migration masih perlu di-commit ke
  git di titik aman.
- Dibersihkan juga: 4 script deploy (`deploy_vps.py`, `check_vps_full.py`,
  `scripts/_find_vps.py`, `scripts/_vps_pull_backend.py`) yang masih menunjuk
  IP/port/password VPS lama (`.40:40186`) — diupdate ke VPS aktif
  (`.44:44156`), dan logic deploy diganti dari asumsi systemd ke Docker
  Compose sesuai arsitektur VPS sebenarnya.
