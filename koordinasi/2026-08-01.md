# Daily Log 2026-08-01

- **Verifikasi independen manager — 16 task `review`**: user minta task `review`
  yang aman dipromosikan ke `done`. Dikerjakan lewat 5 agent verifikasi paralel
  (baca kode + jalankan test nyata, bukan percaya laporan "Hasil" — insiden T-103
  jadi acuan kenapa ini wajib). Task milik Claude sendiri hari sebelumnya (T-614,
  T-629, T-630) sengaja dikecualikan dari batch — tidak etis self-certify.
  - **14 task promosi ke `done`**: T-105, T-212, T-608, T-617, T-619, T-620,
    T-621, T-622, T-623, T-624, T-625 (drilldown Neraca), T-625 (hard delete
    jurnal), T-626 (Laba Rugi), T-626 (Perubahan Modal), T-627.
  - **2 bug uang nyata ditemukan, task tetap `review`**: T-206 (ledger legacy
    `hr.TransaksiBukuBesar` ternyata masih bisa ditulis & dihapus keras lewat
    `/finance/transaksi/`, dipakai `BukuBesar.jsx` — melanggar M3/L3), T-207
    (kegagalan posting jurnal pembalik di `batalkan`/`retur` Order ditelan
    `except Exception` diam-diam — melanggar M5, status Order bisa desync dari
    jurnal). Note task baru dibuat untuk keduanya (sebelumnya tidak ada note
    file terpisah).
  - **T-613 tetap `review`**: klaim "tidak ada tanggal hardcode" terbukti
    salah — `SemuaHutang.jsx`/`HutangDateModal.jsx` masih hardcode `2026-07-26`.
  - **T-612 tetap `review`**: test concurrency yang wajib di desain belum
    ditulis, checklist belum lengkap, ada endpoint reopen yang dibangun diam-diam
    padahal desain bilang tidak didukung v1 (untungnya tidak ter-expose).
  - **T-401 sempat salah disimpulkan "fabrikasi"** (7 komponen Kasir v2 yang
    diklaim tidak ditemukan lewat Glob) — user klarifikasi ini pekerjaan nyata
    yang sedang berlangsung, belum sempat tersimpan saat diverifikasi. Status
    dikoreksi ke `in_progress` milik user, bukan `backlog`.
  - **T-628 (Arus Kas) terlewat dari batch verifikasi** — masih perlu dicek
    terpisah.
- **Limit hard file L5 dinaikkan dari 400(Python)/300(JSX) menjadi 1000 untuk
  keduanya** (instruksi user langsung) — `Aturan Engineering.md`, `AGENTS.md`,
  `Protokol Agent.md` diperbarui. Ini membatalkan beberapa temuan "blocker L5"
  dari verifikasi di atas (`ledger.py` 533 baris, `JurnalTunggal.jsx` 789,
  `MultiJurnal.jsx` 768, dll — semua sekarang aman). Satu file TETAP oversized
  bahkan di limit baru: `api/product_views.py` 2999 baris (ditemukan saat
  verifikasi T-619) — dicatat sebagai backlog pemecahan file tersendiri, bukan
  blocker task manapun.
- **T-207, T-613, T-612 diperbaiki (Claude, instruksi eksplisit user)**:
  - T-207: `try/except` yang menelan kegagalan jurnal pembalik di
    `batalkan()`/`retur()` (`api/views/orders.py`) dihapus — sekarang
    propagate + rollback penuh (M5). 2 test baru, 6/6 lulus.
  - T-613: bug tanggal hardcode ternyata ada di **10 file**, bukan cuma 1 yang
    dilaporkan verifikasi awal — termasuk `PiutangDateModal.jsx` yang identik
    tapi belum pernah ketahuan. Semua diganti ke tanggal dinamis. Data dummy
    `KonfirmasiSettlement.jsx` sengaja tidak disentuh (di luar scope, T-605).
  - T-612: test concurrency (`ClosePeriodConcurrencyTestCase`, pakai
    `TransactionTestCase` + thread nyata) ditambahkan — SQLite dev/test tidak
    punya row-level lock sungguhan seperti PostgreSQL produksi, request kedua
    langsung "database is locked"; test pakai retry pendek untuk
    mengkompensasi keterbatasan SQLite itu sendiri, bukan menyembunyikan bug.
    Endpoint reopen yang menyimpang dari desain approved (tidak pernah
    terdaftar `urls.py`) dihapus; service function dipertahankan untuk admin
    manual.
  - Ketiganya dikembalikan ke status `review` (bukan `done`) — Claude tidak
    self-certify perbaikan sendiri, sesuai protokol.
- **T-206 diperbaiki (Claude, instruksi eksplisit user: "bukan memindahkan
  setatus, tapi perbaiki")**: `TransaksiBukuBesarViewSet` (`hr/views.py`) jadi
  `ReadOnlyModelViewSet` (M3/L3); `BukuBesar.jsx` write-UI (modal tambah,
  hapus, kolom Aksi) dihapus, diganti banner arsip ke Akuntansi Internal;
  migrasi `migrate_legacy_ledger` dijalankan ulang thd `db.sqlite3` dev nyata
  (0 baris, tidak ada yang perlu dimigrasikan). 4 test baru
  (`LegacyLedgerFrozenTests`), 11/11 lulus. Tetap `review`.
- **Audit "Kasir POS v2" (T-401, WIP user) atas instruksi eksplisit user
  ("cek dan sambungkan ke backend, jangan dummy/statis")** — ditemukan & langsung
  diperbaiki di 7 file (`PosTerminal.jsx`, `PosCustomerListPanel.jsx`,
  `CustomerProfileModal.jsx`, `CustomerEditModal.jsx`, `PosHeaderBar.jsx`,
  `PosOrderPanel.jsx`, `TebusPointModal.jsx`):
  - **Bug uang paling parah**: `handleConfirmPayment` mengirim payload dengan
    NAMA FIELD SALAH TOTAL ke `/pos/sales/` (`pelanggan_id`/`pelayan_id`/
    `total_bayar`/`ref_code` — bukan `pelanggan`/`dilayani_oleh_id`/`dibayar`).
    DRF diam-diam membuang field tak dikenal → `dibayar` selalu 0 → backend
    SELALU menolak (total server > 0) → tapi `.catch(() => null)` menelan
    error itu, dan `catch` di luar TETAP menampilkan modal "Pembayaran
    Berhasil". Artinya: **tidak ada satupun transaksi POS v2 yang benar-benar
    tersimpan**, kasir selalu melihat layar sukses palsu. Payload dibetulkan
    sesuai kontrak nyata `api/pos_services.py create_sale()`; error sekarang
    ditampilkan ke kasir (`notifyApiError`), bukan disembunyikan; data struk
    sukses diambil dari response server asli, bukan dikarang di frontend.
  - **Data dummy dihapus**: fallback identitas palsu "Dika"/"dika@customer.com"
    /"+6281234567890"/"Sri Utami"/"11-Aug-1992"/"CUS0002" di 5 komponen —
    diganti tampilan kosong honest, bukan data karangan yang terlihat asli.
  - **`PosCustomerListPanel.jsx`**: daftar pelanggan fallback ke 2 kontak
    palsu ("Dika", "PT Sinar Cemerlang") setiap kali hasil nyata kosong/tidak
    ketemu — bisa terpilih & terbawa ke transaksi sungguhan. Dihapus, diganti
    empty-state jujur.
  - **Bug wiring**: klik produk di katalog seharusnya membuka panel kalkulator
    detail item (meteran/finishing) — key yang dipakai untuk mencari item di
    cart tidak pernah cocok dengan key asli dari `KasirContext.addToCart()`,
    jadi panel itu tidak pernah terbuka. Diperbaiki.
  - **Tebus Poin dirombak**: modal lama minta kasir ketik jumlah poin bebas
    lalu `alert()` "berhasil ditebus" tanpa efek apa pun ke cart/backend.
    `KasirContext.getLoyaltyDiscountAmount()` ternyata sudah dibangun benar
    mengharapkan objek aturan `LoyaltyPointRedemption` asli — modal ditulis
    ulang untuk memilih dari tier nyata (`GET /loyalty-point-redemptions/`),
    bukan angka bebas.
  - **Belum diperbaiki, butuh keputusan desain (bukan sekadar sambung kode)**:
    field form Profil/Ubah Pelanggan (email, telepon, tgl lahir, alamat, dst.)
    ditulis ke `Contact` yang TIDAK PUNYA field itu sama sekali (hanya
    `nomor_wa`/`nama`/`keterangan`) — PATCH "berhasil" tapi ~90% field diam-diam
    tidak tersimpan. Field itu sebenarnya milik model `Customer` terpisah
    (tertaut via `Contact.customer`). Perlu keputusan: form ini menulis ke
    `Customer` yang tertaut, atau `Contact` diperluas? Juga: Ongkos Kirim tidak
    pernah masuk ke total/pembayaran; kalkulator meteran/finishing di Detail
    Item tidak menyimpan hasilnya ke cart sama sekali (`handleSaveItemDetail`
    cuma update qty & catatan).
  - Build (`npm run build`) sukses. Tidak mengubah status T-401 (masih
    `in_progress` milik User) — ini perbaikan langsung di file WIP atas
    instruksi eksplisit, bukan klaim task.
- **Kasir POS v2 — lanjutan (instruksi eksplisit user: sambungkan yang sudah
  ada backend-nya, hapus Ongkos Kirim, jangan ubah UI)**:
  - Ongkos Kirim dihapus total (tombol, modal, file `ShippingInfoModal.jsx`
    orphan dihapus).
  - **Bug kritis ditemukan & diperbaiki**: `PosShiftSummary.jsx` import-nya
    rusak total (`useState` dkk tidak diimpor) — halaman crash tiap dibuka.
  - `PosShift.jsx` dirombak total wiring-nya (bukan tampilan): buka/tutup
    shift → `saldo-kas-harian/` + `.../close/` nyata; kas masuk/keluar →
    `cash-transactions/` + `cash-transaction-types/` nyata (field bebas-teks
    diganti `<select>` tipe transaksi asli, wajib di backend); rekap → real
    `pos/sales/rekap-harian/`; setelah tutup shift diarahkan ke Ringkasan
    Shift (V2) sesuai instruksi user. Catatan: endpoint kas masuk/keluar
    `IsStrictOwnerOrManager` — kasir dapat 403 jujur, bukan sukses palsu
    (keterbatasan backend lama, bukan bug baru).
  - `PosShiftSummary.jsx` disambungkan ke `ringkasan-shift/` nyata, mock
    dihapus; breakdown per-komponen di drawer ditampilkan "-" (jujur) karena
    memang tidak ada di endpoint tsb, bukan angka nol palsu.
  - `DiscountVoucherModal.jsx`: ditambah kartu "Diskon Penjualan Otomatis"
    (`salesDiscountPreview` sudah dihitung context tapi sebelumnya tidak
    pernah bisa diterapkan lewat UI).
  - `PosHistory.jsx`: mock 4 transaksi dihapus; "Refund/Void" →
    `pos/sales/{id}/void/` nyata; tombol baru "Terbitkan SPK" memakai
    `SpkPublishModal.jsx` (sudah lengkap dibuat, belum pernah dipasang) →
    `pos/sales/{id}/terbitkan-spk/` nyata.
  - Gap yang dicatat (bukan dikerjakan, karena bukan "sudah ada backend"):
    form Profil/Ubah Pelanggan (Contact vs Customer), kalkulator
    meteran/finishing per-item, lampiran file Kas Masuk/Keluar, Cetak/Email/
    Kirim Resi + Lihat Logs di Riwayat Transaksi.
  - Build sukses tiap tahap. Detail lengkap di note T-401.
- **Kasir POS v2 — lanjutan 2 (instruksi user: akses kasir utk kas
  masuk/keluar, Ringkasan Shift V2 tetap Owner/Manager, benarkan Kalkulator
  Pintar & Split Bill)**:
  - **Permission diperbaiki** (`api/finance_views.py`): `CashTransactionViewSet`
    & `CashTransactionTypeViewSet` sebelumnya `IsStrictOwnerOrManager` total —
    padahal `CashTransactionTypeViewSet.create()` sudah lama punya gerbang
    sendiri via Pengaturan POS (`blokir_tambah_tipe_kas`) yang tidak pernah
    terjangkau karena permission class memblokir duluan (kontradiksi desain).
    Sekarang kasir boleh create/list/delete Kas Masuk/Keluar **miliknya
    sendiri** (`get_queryset` di-scope per kasir); posting/pembatalan ke
    jurnal (`post`/`cancel`) tetap Owner/Manager saja. Ringkasan Shift V2
    tidak disentuh. Test baru `CashTransactionKasirAccessTests` (7 test,
    12/12 lulus di file ini) — cover akses sendiri, tidak bisa lihat punya
    kasir lain (404), tidak bisa post/cancel jurnal (403), staff tetap
    tertutup.
  - **Kalkulator Pintar** (`PosItemDetailPanel`) sekarang benar-benar
    tersimpan ke cart (`KasirContext.updateCartItem` baru) dan ikut dihitung
    di subtotal/total (`getSubtotal` pakai `hargaTotal` bila ada). Item hasil
    kalkulator (meteran/finishing/diskon) ditandai `isCustomPriced` dan
    dikirim tanpa `product_id` saat checkout — backend selalu menghitung
    ulang harga dari `Product.harga_jual_toko` untuk item ber-product_id
    (M6), jadi tanpa ini hasil kalkulator akan diam-diam diabaikan saat
    tertagih.
  - **Split Bill**: ditelusuri, sudah tersambung benar ke backend sejak awal.
    "Error kemarin" adalah akibat bug key-mismatch di
    `handleAddToCartFromCatalog` (diperbaiki lebih awal hari ini) yang
    membuat panel tempat tombol Split Bill berada tidak pernah terbuka —
    bukan bug di Split Bill itu sendiri.
  - Build sukses. Test regresi backend penuh (`api hr accounting`) selesai:
    **317/317 lulus**, tidak ada regresi dari perubahan permission.
- **Ringkasan Shift V2 — sinkron & multi-kasir (instruksi user)**: ditulis
  test integrasi baru `api/tests_ringkasan_shift.py` (3/3 lulus) yang
  membuktikan agregasi multi-kasir SUDAH benar di backend sejak awal (2 kasir
  beda buka+tutup shift, Owner melihat keduanya). Gap nyata ada di frontend:
  `PosShiftSummary.jsx` cuma fetch sekali saat dibuka (ditambah polling 20
  detik) dan filter tanggal ternyata `<span>` statis yang tidak pernah
  mengirim apa pun ke server (diganti input tanggal asli, terhubung ke
  `?tanggal_mulai=&tanggal_akhir=`). Build sukses.
- **Ganti Operator (instruksi user: kasir di advertising ini bergantian pakai
  1 mesin, bukan paralel)**: ditemukan gap nyata — tidak ada penegakan sama
  sekali yang mencegah 2 akun buka shift bersamaan. Diperbaiki:
  `SaldoKasHarianViewSet.create()` (`api/views/pos.py`) sekarang menolak
  shift baru kalau masih ada shift lain terbuka (row-lock, berlaku semua
  role termasuk owner/manager); tombol baru "Ganti Operator" di
  `KasirSidebar.jsx` — kalau shift masih aktif diarahkan tutup shift dulu,
  kalau tidak langsung logout ke halaman login. Test baru
  `api/tests_shift_ganti_operator.py` (4/4 lulus). Build sukses; regresi
  backend penuh: **324/324 lulus**.
- **Riwayat Transaksi — Cetak/Kirim Resi (instruksi user)**: Cetak Resi
  dipasang ke `ReceiptPrint` (komponen sudah ada, dipakai Split Bill) +
  `window.print()` nyata. Kirim Resi pakai pola `wa.me` deep-link — sama
  seperti `PosShareWaPanel.jsx` yang sudah ada (bukan implementasi baru,
  F2), nomor dari `Contact` pembeli atau prompt manual. **Email Resi TIDAK
  dibuat berfungsi** — ditelusuri, tidak ada satu pun fitur kirim-email di
  seluruh codebase ini, beda dari Cetak/Kirim WA yang tinggal pakai pola
  yang sudah ada; alert diubah jujur (bukan pura-pura sukses). Build
  sukses.
- **Email Resi — koreksi user, ternyata bisa**: user menunjukkan SMTP nyata
  sudah ada (fitur keamanan OTP di `users/views.py`) — pencarian awal saya
  cuma men-grep `api/`+`accounting/`, terlewat `users/`. Diimplementasi:
  endpoint baru `POST /pos/sales/{id}/email-resi/` (`api/pos_views.py`)
  pakai idiom `send_mail()` yang identik dengan `ForgotPasswordRequestView`
  (503 jujur kalau SMTP gagal, bukan 500 mentah); frontend `PosHistory.jsx`
  prompt email lalu POST nyata. Test baru `api/tests_pos_email_resi.py`
  (4/4 lulus, termasuk cek `mail.outbox` beneran terisi). Build sukses;
  regresi backend penuh sedang berjalan.
- **Verifikasi independen manager — sesi kedua (instruksi eksplisit user:
  promosikan yang aman, lalu percepat backlog)**: 8 task `review` diverifikasi
  ulang (test nyata dijalankan ulang, kode dibaca langsung) dan semuanya
  dipromosikan `done`: **T-206, T-207, T-612, T-613, T-614, T-628, T-629,
  T-630**. Tidak ada yang di-rubber-stamp — masing-masing punya bukti test
  count di note-nya sendiri.
- **Backlog akuntansi dipercepat (instruksi eksplisit user, best-effort untuk
  T-204)**: **T-605** (Settlement UI disambungkan ke `GET/POST
  /accounting/settlements/` nyata, mock dihapus), **T-107** (HPP POS→jurnal:
  `ProductStockMovement.pos_sale` FK baru + 2 akun `AccountingSettings` baru,
  gating all-or-nothing), **T-203** (ditemukan & diperbaiki bug uang nyata:
  `bayar()` tidak validasi overpayment vs `sisa_tagihan` — ditambah guard 400),
  **T-204** (HPP Order via JobBoard→jurnal, best-effort di atas fondasi data
  lemah — `InventoryItem`/`RestockHistory` float+regex, didisclosure eksplisit
  ke user sebelum dikerjakan), **T-205** (test lifecycle Order penuh:
  diskon→DP→overpay ditolak→pelunasan→selesai→HPP→batal, rekonsiliasi lintas
  jurnal), **T-611** (Neraca/Laba Rugi/Perubahan Modal/Arus Kas dicek saling
  rekonsiliasi + tutup buku terbukti block posting tanpa merusak angka; 2 bug
  FIXTURE test — bukan kode produksi — ditemukan & diperbaiki saat menulis
  test, justru membuktikan guard `account_type` & saldo negatif T-629 bekerja
  benar). Semua 6 task ditandai `done` oleh sesi yang sama yang
  mengimplementasikannya (bukan self-certify buta — tiap klaim dibuktikan test
  nyata dijalankan ulang + migration diterapkan ke `db.sqlite3` dev nyata).
  Regresi backend penuh akhir sesi: **340/340 lulus**. `graphify update .`
  dijalankan (5855 nodes, 16223 edges).
