# 2026-08-10

- T-721: Atas arahan manager, router AI penuh dibatalkan. VPS memakai alur Evolution dan `ProductPrice` lama; AI hanya memberi respons pertama. Test WA lama 12/12 dan health VPS `ok`; menunggu review independen.
- T-720/T-722: User lapor Antrean WA "berantakan" (layout item, harga selalu 0, tombol kirim invoice tidak ketemu). Manager (Claude) perbaiki & deploy langsung: root cause harga 0 = polling 15 detik menimpa edit aktif kasir; layout tabel item diperbaiki; tombol invoice dipindah ke bawah kartu SPK. Build+eslint lulus, deploy VPS, health 200.
- Faktur pesanan lunas kini bisa dikirim langsung via WA (tombol "Kirim Faktur WA") di `PelunasanModal` dan `InvoiceModal` — otomatis berlaku di Pesanan & Pelunasan maupun Antrean WA (komponen sama). Ditambahkan juga kotak pencarian pesanan (nomor/nama/no. WA) di halaman Pesanan & Pelunasan. Build+eslint lulus, deploy VPS, health 200.
- Invoice WA (`order_invoice_whatsapp.py`) diperkaya field-nya menyamai format SPK papan kerja (Ukuran P×L, Kontak WA, catatan "Dibuat langsung di Kasir" + ID tracking untuk order `sumber != wa`), bukan format baru. Kasir sekarang bisa Tambah Pelanggan (member+loyalty) & Tambah Supplier langsung dari Kasir (`/kasir/pelanggan-supplier`, komponen form direuse dari modul Pelanggan & Supplier) — atas instruksi eksplisit user, izin `SupplierViewSet` dibuka untuk kasir (sebelumnya read-only). 21/21 test backend lulus, build+eslint frontend lulus, deploy VPS (backend+frontend rebuild), health 200.
- User tunjuk 3 PDF referensi (RESI/INVOICE/SPK format `GlobalListPanel.jsx`, jauh lebih rapi dari hasil reportlab sebelumnya) dan minta dokumen yang dikirim lewat WA memakai format itu, bukan bikin baru. `susun_invoice_dp_pdf` ditulis ulang total jadi A4 profesional (header identitas bisnis dari `SystemConfig`, tabel NO/DESKRIPSI/UKURAN/QTY/TOTAL, kotak Total/DP/SISA TAGIHAN dengan badge LUNAS hijau, blok tanda tangan Pelanggan/Finance) — dirender & dicek visual lokal, hasilnya cocok dengan referensi. `susun_resi_pdf` (resi POS via WA) ditambah header identitas bisnis + Pelanggan/No.WA, dan bug format Rupiah (koma bukan titik, tidak konsisten dengan seluruh app) diperbaiki di kedua dokumen. 30/30 test backend lulus, deploy VPS, health 200.
