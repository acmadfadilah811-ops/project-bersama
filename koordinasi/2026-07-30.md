# Daily Log 2026-07-30

- **T-619 (Claude)**: Memperbaiki `Pembelian.jsx` (Transaksi POS) — aksi Post/Batal Post/Post Pembayaran/Log yang sebelumnya palsu (mutasi state lokal + toast sukses fiktif) dihapus, diganti toolbar bulk yang memanggil endpoint nyata (`workflow/update-status`, `workflow/toggle-payment`) dengan kelayakan per baris mengikuti guard backend. Klik baris membuka `PembelianDetail` asli.
- **T-619 (Claude)**: Investigasi menemukan bug backend nyata (di luar scope file T-619, dikerjakan atas instruksi eksplisit user): `PurchaseWorkflowView.update_status()` untuk status "Selesai" tidak pernah memposting stok/jurnal — hanya mengubah label status. Diperbaiki dengan fungsi bersama `post_stock_in_document()` (ekstrak dari `StockInDocumentViewSet.post_document`, dipakai ulang bukan duplikasi); "Batal" lewat dropdown kini ditolak bila sudah diterima (harus lewat Retur), menyamakan guard `batalkan()`. +5 test regresi, suite `api`+`accounting` 224 test (223 lulus, 1 gagal pre-existing tak terkait).
- **T-619 (Claude)**: Audit Return Pembelian/Return Penjualan — sudah API nyata, tidak ada temuan data/tombol palsu.
- **T-622 (Claude)**: User tanya apakah Pengaturan Supplier & Simpanan Pelanggan sudah terhubung backend — ternyata 100% mock (tidak ada `apiClient` sama sekali di kedua file). Agent lain sedang off, user minta Claude langsung eksekusi (di luar peran manager biasa, atas instruksi eksplisit). Disambungkan ke data asli: `Customer.deposit` (field lama yang belum pernah dipakai UI) untuk Simpanan Pelanggan; 2 field baru `akun_hutang`+`jatuh_tempo_hari` di `Supplier` (migration `api/0095`) untuk Pengaturan Supplier. Test baru 3/3 lulus, suite `api`+`accounting` 80/80 lulus 0 regresi, `npm run build` lulus. Status `review` — belum `done`, menunggu verifikasi visual browser oleh user sendiri.
- **Follow-up styling (Claude)**: user minta hilangkan garis tepi kotak (card border) di 4 halaman Akuntansi (Pengaturan Supplier, Simpanan Pelanggan, Pelanggan Jatuh Tempo, Uang Muka Pembelian) supaya lebih profesional. Murni CSS, `npm run build` lulus lagi.
- **T-610 koreksi (Claude)**: User minta menu Biaya (`DaftarBiaya.jsx`) memuat data asli. Ternyata klaim lama T-610 ("terhubung API") salah — masih 100% mock, dan file di disk sudah dalam kondisi hasil restore UI lama T-624 (Codex, `review`). Disambungkan ke `/api/accounting/accounts/` (filter `account_type=expense`, reuse `FilterAkunModal` yang sudah ada) untuk list, dan reuse `RincianMutasiAkun`+`/api/accounting/ledger/<id>/` (dipakai `BukuBesar.jsx`) untuk drill-down per akun — bukan bikin plumbing baru. User konfirmasi urutan T-624→Claude ini memang disengaja, bukan tabrakan kerja. Follow-up UX: klik untuk buka detail sekarang HANYA di sel Saldo, bukan seluruh baris.
- **Bugfix saldo berjalan (Claude)**: user kasih contoh data referensi Olsera (90.190→250.190→9.610.190) untuk Rincian Mutasi Akun, yang membuktikan bug uang nyata: `RincianMutasiAkun.jsx` & `MutasiTable.jsx` (Kas & Bank, T-604) menebak arah saldo berjalan sendiri via `isDebitNormal`, padahal field yang dipakai (`account.classification`) tidak pernah dikirim endpoint ledger — selalu `false`, salah untuk semua akun. Backend (`get_account_line_history`) ternyata sudah lama benar (pakai `account.normal_balance` asli) dan sudah kirim `running_balance` per baris, cuma tidak pernah dipakai frontend. Fix: frontend pakai `row.running_balance` server langsung. Sekalian: `pelanggan_supplier` diberi prefix "Pelanggan: "/"Supplier: " (sebelumnya nama polos, tidak sesuai contoh Olsera & tidak sesuai asumsi filter dropdown yang sudah ada). Test baru `accounting/tests_ledger_running_balance.py` (3/3, meniru persis angka Olsera), suite `accounting` 88/88 lulus, `npm run build` lulus. Dampak silang ke T-604 (Kas & Bank) yang sudah `done` — dicatat di board.
- **Kolom Aksi di Rincian Mutasi Akun (Claude)**: user kasih contoh popup "Pasangan Jurnal" Olsera — ternyata `PasanganJurnalModal.jsx`/`HapusTransaksiModal.jsx` sudah ada persis sesuai contoh (dipakai Kas & Bank), tinggal reuse ke `RincianMutasiAkun.jsx` (dipakai Buku Besar & Biaya) yang belum punya kolom Aksi. Sekalian dibereskan pelanggaran lama L5: file itu sudah 590 baris sebelum disentuh — dipecah jadi 3 komponen (`RincianMutasiAkunHeader/FilterBar/Table.jsx`), halaman utama turun ke 285 baris. `npm run build` lulus.
- **Investigasi "kenapa Biaya Rp 0" (Claude)**: dicek via API + DB langsung — BUKAN bug. 28 jurnal nyata di dev DB cuma menyentuh Hutang Dagang/Kas (pembayaran PO) dan Persediaan (stok masuk, metode perpetual) — tidak ada satupun yang pernah menyentuh akun Biaya. HPP baru terisi kalau ada pengakuan HPP saat penjualan (belum dibangun, backlog **T-107**). Bukan salah UI/API.
- **Data dummy demo (Claude, atas permintaan eksplisit user)**: post 5 jurnal via `create_journal_entry()` (M2) berlabel `[DUMMY]` di deskripsi, tanggal 2026-07-30, biar user bisa lihat output UI Biaya dengan angka asli: JU-202607-0029..0031 (Pembelian ke 50000/21000, pakai 3 supplier asli, angka 90.190/160.000/9.360.000 — persis contoh Olsera user, running balance akhir 9.610.190 di akun Pembelian), JU-202607-0032 (Biaya gaji 4.500.000, ke 60100/11101), JU-202607-0033 (HPP 320.000, ke 51000/11400).
- **X3 — TEMUAN BUG KEAMANAN/UANG, WAJIB eskalasi ke manager (B5), TIDAK diperbaiki di sesi ini**: waktu cek cara membersihkan data dummy di atas, ditemukan `JournalEntryDetailView.delete()` (`accounting/views/journal.py:181-184`, dipakai tombol "Hapus" yang baru saya sambungkan ke `RincianMutasiAkun.jsx` hari ini, dan sudah lama dipakai Kas & Bank) melakukan **`entry.delete()` — HARD DELETE langsung** pada `JournalEntry` berstatus `posted`, TANPA membuat jurnal pembalik dan TANPA mencatat `reason` yang dikirim `HapusTransaksiModal.jsx` ke mana pun (parameter `reason` tidak pernah dibaca `request.data`). Ini pelanggaran **L7** ("Jurnal berstatus `posted` tidak pernah di-edit atau di-delete") dan **M7** (pembatalan wajib jurnal pembalik, bukan hapus). Bug ini SUDAH ADA sebelum hari ini (bukan bikinan task ini) dan berlaku untuk SEMUA jurnal posted di seluruh aplikasi (Kas & Bank, Biaya, Buku Besar) — bukan cuma data dummy. Perlu keputusan manager: apakah `delete()` diubah jadi buat jurnal pembalik (pola `reverse_purchase_payment_journal` yang sudah ada), dan alasan `reason` disimpan ke `JournalAuditLog`. **User diberi tahu langsung di chat** agar tidak memakai tombol Hapus dulu untuk data asli sebelum ini diperbaiki.
- **Bug di Invoice.jsx (Claude)**: user minta cek bug + dummy data di Invoice. Ditemukan 2 bug mekanis (bukan mock — endpoint sudah asli tapi kontrak param salah): (1) kirim `start_date`/`end_date`, backend cuma baca `date_from`/`date_to` → filter tanggal selalu default hari ini apapun yang dipilih; (2) kolom Tanggal/Debit/Kredit pakai nama field yang tidak ada di serializer asli (`row.tanggal`/`row.nilai_debit`/`row.nilai_kredit`) → selalu tampil "-"/Rp 0. Diperbaiki: pakai `date_from`/`date_to` dan `row.date`/jumlah `lines[].debit`/`kredit`.
- **Filter kategori Invoice dipetakan penuh (Claude, atas permintaan user "semua kategori ada datanya dan filter berfungsi")**: 16 label Indonesia dipetakan ke `source_type`/`is_reversal`/`description_prefix` asli backend. Backend `_filter_journal_queryset` (dipakai bersama Jurnal Umum+export) ditambah dukungan `source_type` koma-list, `is_reversal` (pakai `reversed_entry`), `description_prefix` — aditif, tidak mengubah pemanggil lama. Test baru 4/4 lulus, suite `accounting` 92/92 lulus. 9 data dummy tambahan dibuat (semua lewat `create_journal_entry()`/`post_cash_transaction_journal()` asli) supaya kategori yang belum pernah ada transaksinya (Penjualan, Pembayaran Penjualan, Retur, Pemasukan/Pengeluaran, Stok Opname/Keluar, Produk Inventori) kebagian contoh. Ke-16 kombinasi filter diverifikasi lewat `APIClient` nyata, semua mengembalikan data. Bonus temuan: "Transfer Modal" dan sebagian "Retur Pembelian" ternyata SUDAH ADA datanya dari histori pemakaian aplikasi asli (bukan dummy).
- **Laba Rugi Satu/Multi Periode dibangun + temuan dual-source (Claude, 2026-07-31)**: user minta lanjut ke Laba Rugi (Akuntansi) + sinkronkan dengan Laba Rugi di Laporan Penjualan. Kedua halaman Akuntansi (`LabaRugiSatuPeriode.jsx`/`LabaRugiMultiPeriode.jsx`) ternyata 100% mock (fetch ke `/accounting/ledger/` tapi hasilnya dibuang, array akun hardcode semua 0). Dibangun backend baru: `get_income_statement()` (`accounting/services/ledger.py`) — P&L per **klasifikasi COA** (Pendapatan/HPP/Pengeluaran/Pendapatan Lain/Pengeluaran Lain), bukan kode akun hardcode, dari `accounting.JournalEntry` asli. Akun kontra (Return/Potongan) otomatis bernilai negatif by design (arah dasar account_type, sengaja TANPA ikut flip `is_contra` seperti `normal_balance`) supaya subtotal flat-sum tetap benar. Endpoint baru `GET /accounting/reports/income-statement/`. Frontend disambungkan: Satu Periode 1x call, Multi Periode 3x call paralel (1 per bulan quarter) + digabung client-side. Test baru 5/5 lulus (termasuk kasus akun kontra), suite `accounting` 104/104 lulus, `npm run build` lulus.
  **Follow-up drill-down (Claude)**: user kasih contoh detail — nominal akun di Laba Rugi harus biru+klik ke Rincian Mutasi Akun, kolom "Nama Toko" (bukan Pelanggan/Supplier terpisah), info pembeli/supplier jadi sub-baris di Deskripsi ("Pembeli: X"), filter +opsi "Tgl Transaksi". Dikerjakan: backend `_income_statement_section()` +`id` akun, `get_account_line_history()` +`processed_by_name`, label "Pelanggan:"→"Pembeli:" (koreksi tebakan lama). `RincianMutasiAkunTable.jsx` (dipakai Buku Besar/Biaya, sengaja tidak sentuh Kas & Bank) kolomnya disesuaikan. `LabaRugiSatuPeriode.jsx` (646 baris, pelanggaran lama) dipecah 4 file, nominal jadi tombol biru buka drill-down. Suite `accounting` 104/104 lulus, `npm run build` lulus, diverifikasi end-to-end via APIClient nyata.
  **T-627 Neraca (Claude)**: user lanjut minta Neraca. Sama pola: 100% mock (699 baris, fetch dibuang). Dibangun `get_balance_sheet()` — saldo kumulatif per klasifikasi COA (Aset Lancar/Tidak Lancar/Kewajiban/Modal), akun kontra otomatis negatif (pola sama T-626), baris sintetis "Pendapatan periode ini" dari `get_income_statement()` karena belum ada jurnal tutup-buku otomatis. Endpoint baru `GET /accounting/reports/balance-sheet/`. Frontend dipecah 3 file (699→159+279+128 baris). Nominal TIDAK dibuat klik (beda dari Laba Rugi, belum diminta). Test baru 5/5 lulus (bukti Neraca balance), suite `accounting` 109/109 lulus, `npm run build` lulus. Diverifikasi end-to-end: Total Aset = Total Kewajiban+Modal persis sama (-Rp 2.768.571.400) memakai data dev asli yang sudah menumpuk dari banyak sesi.
  **Temuan sinkronisasi**: dibandingkan hasil Akuntansi (ledger-based) vs Laporan Penjualan `/reports/laba-rugi/` (`rpt_laba_rugi`, sudah lama ada & real, tapi hitung LANGSUNG dari Order/POSSale + CashTransaction, TIDAK LEWAT `accounting.JournalEntry` sama sekali) untuk periode yang sama (Juli 2026): angkanya jauh berbeda (Akuntansi: Pendapatan Rp 0, Laba Bersih -Rp 13.955.000 vs Laporan Penjualan: Pendapatan Rp 2.704.000, Laba Bersih +Rp 1.669.000). **User memutuskan (setelah cek Olsera — di sana pun berbeda)**: biarkan terpisah, tambahkan disclaimer di kedua sisi (`LabaRugiSatuPeriode.jsx`/`LabaRugiMultiPeriode.jsx` & `LaporanProduk.jsx::LabaRugiTable`) supaya user aplikasi tidak bingung. Tidak ada perubahan logika, murni teks. `npm run build` lulus.
- **Log Jurnal dibangun + T-625 diperbaiki (Claude, 2026-07-31)**: user minta lanjut ke Log Jurnal, kasih contoh format Olsera persis (deskripsi "Debit/Kredit akun ... IDR x" dipisah ' | ', popup rincian saat No Transaksi diklik). Ditemukan `LogJurnal.jsx` terhubung ke endpoint SALAH TOTAL (`/accounting/lifecycle-logs/` — log Start/Stop Akuntansi, bukan Log Jurnal) dengan banyak fallback tebak-tebakan (nama "Brandy" hardcode). Ini terhubung langsung ke bug T-625 (hard-delete jurnal posted) yang ditemukan sebelumnya — ditanya ke user via pilihan eksplisit, dipilih: jurnal pembalik (bukan hard-delete Olsera-persis), Log Jurnal tetap tampil format sama. Dibangun: field `JournalAuditLog.Action.DELETED` baru, service `build_deletion_note()`, `JournalEntryDetailView.delete()` ditulis ulang (wajib `reason`, jurnal pembalik via `create_journal_entry()`, cegah hapus dobel), endpoint baru `GET /accounting/journal-audit-logs/` (sebelumnya tidak ada endpoint list untuk `JournalAuditLog` sama sekali), `LogJurnal.jsx` ditulis ulang ke endpoint asli + dipecah 4 file (L5, file lama 539 baris), komponen baru `LogJurnalLinesModal.jsx` untuk klik No Transaksi. Test baru 7/7 lulus, suite `accounting` 99/99 lulus, `npm run build` lulus. Diverifikasi end-to-end: 1 jurnal Pembelian dummy dihapus lewat API asli, note yang dihasilkan persis format yang diminta user. T-625 pindah dari `blocked` ke `review`.
- **Bug kritis menu Aset — migration belum ter-apply (Claude)**: user minta cek bug + data dummy di menu Aset (T-623, Codex, `review`). Ditemukan `accounting.0031_fixed_asset_register` belum pernah `migrate` ke `db.sqlite3` dev asli — cuma lolos test karena `manage.py test` selalu pakai DB sementara yang di-migrate dari nol. Fitur Aset akan **error 500 total** di app nyata sebelum ini. Diperbaiki: `manage.py migrate accounting` (aman, cuma bikin tabel baru, tidak ada migration lain yang ketinggalan di app manapun — sudah dicek). Verifikasi end-to-end via APIClient nyata: GET/POST `/api/accounting/assets/` sukses. Data dummy dibuat via `create_fixed_asset()` asli (bukan bypass): `AST-DUMMY-001` Laptop Kantor Rp15jt (Peralatan/Akum. Penyusutan Peralatan/Kas), `AST-DUMMY-002` Mesin Produksi Rp85jt (Aset Tetap/Akum. Penyusutan Aset Tetap/Hutang Dagang) — jurnal `JU-202607-0034`/`0035`. Ditambahkan 1 item baru ke checklist Definition of Done [[Aturan Engineering]]: migration baru wajib dicek `showmigrations` ke DB dev asli, bukan cuma andalkan test suite.
- **T-623 (Codex)**: Register aset persisten, jurnal perolehan atomik/idempoten, import CSV preview/commit, export Excel/PDF, dan pemecahan UI Aset selesai diimplementasikan. Test khusus 4/4, check/migration check, ESLint, dan build frontend lulus. Status `review`; penyusutan otomatis dan pelepasan aset dipisahkan sebagai scope lanjutan.
