- Dibuat [[Project Overview]] — ringkasan arsitektur Bintang ERP (backend + frontend) berdasarkan `PROJECT_KNOWLEDGE_BASE.md` dan hasil scan Graphify.
- Dibangun struktur koordinasi multi-agent: [[Agent Board]] + [[Protokol Agent]], 3 epik ([[Integrasi Akuntansi-POS]], [[Integrasi Akuntansi-Orders]], [[Perbaikan Orders]]), dan 3 task verifikasi awal (T-101, T-201, T-301). `CLAUDE.md` diupdate agar semua agent membaca board dulu.
- ❗ Menunggu input user: daftar masalah modul Orders di [[T-301 Inventarisasi masalah Orders]].
- ✅ [[T-101 Verifikasi jembatan POS-Akuntansi|T-101]] selesai (status `review`): penjualan POS **tidak pernah** tercatat ke buku besar — yang ada baru settlement non-tunai (yang mengkredit akun transit kosong). Temuan besar: **dua buku besar paralel** (`hr.TransaksiBukuBesar` legacy vs `accounting.JournalEntry`) → task baru T-206. Graph tidak di-update (read-only, tidak ada kode berubah).
- 📜 Ditetapkan [[Aturan Engineering]] — aturan teknis ber-ID untuk semua executor agent (umum, bugfix, fitur, uang/akuntansi M1–M8, API, DB, testing, ranjau project, larangan keras L1–L10, eskalasi X1–X6). Peran ditata: Claude = manager (aturan/desain/review), agent lain = executor. `CLAUDE.md` & [[Protokol Agent]] diikat ke aturan ini.
- ✅ T-101 difinalkan `done` oleh user. 📐 [[T-102 Desain mapping POS-Jurnal]] ditulis manager: per-sale posting, satu jalur tunai/non-tunai via `PaymentMethod.account`, PPN baris terpisah, gating lifecycle akuntansi, idempotency unique constraint, backfill command. Temuan tambahan: `accounting_payment_method` belum pernah diisi kode mana pun — mapping `POSPaymentMethod` masuk scope T-103.
- ✅ **T-102 di-approve user** (tanpa revisi) → **T-103 SIAP DIKLAIM executor**. Urutan kerja disetujui: **paralel** — backend T-103 jalan, sambil user mengisi requirement kasir.
- 🆕 Epik 4 dibuka: [[Revisi UI Kasir v2]] (replacement terkontrol `PointOfSale.jsx`, bukan versi paralel) + [[T-401 Requirement Kasir v2]] menunggu input user (layar mana, fitur apa saja, keluhan UX).
- 🔍 **Review manager atas T-103 (Antigravity): BELUM di-approve, dikembalikan `in_progress`.** Diverifikasi independen: 5/5 test T-103 lulus, atomicity M5 benar, migration bersih. Tapi 2 temuan blocking: (1) sale ber-pajak bisa 500/rollback total kalau akun PPN belum diatur — melanggar keputusan gating "kasir tak boleh terblokir" (fix: pindahkan cek ke `should_post_sale()`); (2) fallback string `is_cash` bisa menimpa flag akuntansi yang sudah eksplisit False — melanggar T-102 §6. Bonus temuan: 3 test pre-existing gagal (dikonfirmasi via `git stash`, bukan regresi T-103) → [[T-108 Perbaiki test pre-existing]]. Detail lengkap di [[T-103 Implementasi posting POS-Jurnal]] §Hasil Review Manager.
- 🔄 [[T-103 Implementasi posting POS-Jurnal|T-103]] diperbaiki & diajukan ulang untuk `review` manager (Antigravity): Fix 1 (pengecekan `pos_ppn_output_account` dipindah ke gating `should_post_sale()`), Fix 2 (`is_cash` dari `pm_accounting` menjadi otoritatif tanpa tertimpa string), 2 unit test baru ditambahkan (7/7 pass), `graphify update .` tuntas.
- ✅ **T-103 DI-APPROVE `done` oleh manager** setelah verifikasi independen kedua: baca langsung kode fix (sesuai), 7/7 test pos_posting lulus, full suite api+accounting 132 test → persis 3 kegagalan pre-existing yang sama (0 regresi baru), graphify update fresh. **T-104/T-105/T-106/T-107 sekarang siap diklaim** (dependency lepas).
- 🐛 **Temuan user**: bug navigasi di Buku Besar (akuntansi) — klik akun/ikon dokumen balik ke Daftar Akun alih-alih membuka Rincian Mutasi Akun. Dikonfirmasi file yang benar: `features/accounting/pages/BukuBesar.jsx` (bukan versi legacy di `features/finance/`, yang dibekukan per M3). Manager menulis hipotesis: konflik `useSearchParams` antara `BukuBesar.jsx` (param `hide_sidebar`) dan parent `AccountingInternalApp.jsx` (param `active` untuk switch tab) — kemungkinan memicu remount yang me-reset state lokal. Dibuat epik baru [[Bug QA Manual]] (bucket untuk bug temuan manual user, T-108 dipindah ke sini juga) + task [[T-501 Bug navigasi Buku Besar]], siap diklaim, wajib reproduksi dulu sebelum fix (B1).
- 🔄 [[T-501 Bug navigasi Buku Besar|T-501]] diperbaiki & diajukan untuk `review` manager (Antigravity): Root cause hipotesis manager terbukti 100% benar (`useEffect` cleanup & trigger ganda `setSearchParams` di `BukuBesar.jsx` memicu URL race condition di React Router & me-reset `viewingAccountId`). `useSearchParams` dilepas dari `BukuBesar.jsx`, kontrol sidebar disinkronkan murni via React state `onToggleSidebar`. Frontend build `npm run build` sukses (1.83s), file legacy tidak disentuh, `graphify update .` tuntas.
- ✅ **T-501 DI-APPROVE `done`** setelah verifikasi independen: diff bersih & minimal, `npm run build` dijalankan ulang manager → sukses. Manager sekaligus menemukan pola bug `hide_sidebar`/`useSearchParams` yang **identik, belum diperbaiki**, di `ListKasBank.jsx` dan `JurnalUmum.jsx`.
- 🔄 [[T-502 Decouple klik akun dan pola hide_sidebar|T-502]] diperbaiki & diajukan untuk `review` manager (Antigravity): (A) `onClick` pada `<tr>` di `BukuBesar.jsx` dan class `cursor-pointer` dihapus, menjadikan ikon `FileText` satu-satunya trigger; (B) `useSearchParams` dilepas dari `ListKasBank.jsx` dan `JurnalUmum.jsx` untuk eliminasi race condition `hide_sidebar`; audit B1 mengonfirmasi `ListKasBankTable.jsx` tidak memiliki redundansi ikon (detail dibuka dari sel Saldo) dan `JurnalUmum.jsx` tidak memiliki detail akun. Frontend build `npm run build` tuntas (1.63s), `graphify update .` tuntas.
- ✅ **T-502 DI-APPROVE `done`** — user konfirmasi Bagian A langsung di browser; manager verifikasi independen Bagian B (diff 3 file bersih, klaim "tidak ada redundansi" di `ListKasBankTable.jsx` dicek langsung ke kode — benar, hanya sel Saldo yang jadi trigger), `npm run build` diulang → sukses. **Epik Bug QA Manual: 2/2 task tuntas** (T-108 masih backlog).
- 🆕 User bawa masalah baru: `Transaksi → Penjualan` (4 tab: Butuh Diproses/Selesai/Pengembalian/Dibatalkan) perlu detail per-tab (referensi Olsera) + terhubung ke akuntansi. Manager konfirmasi langsung di kode: hanya tab Pengembalian yang sudah punya detail sendiri (`ReturnOrderDetail.jsx`); 3 tab lain masih berbagi `OrderDetail.jsx` generik. Dipecah: [[T-302 Bangun Detail Pesanan Selesai]], [[T-303 Bangun Detail Pesanan Dibatalkan]], [[T-304 Rapikan Open Order Detail]] (Epik 3), plus [[T-207]] baru di Epik 2 untuk jurnal pembalik Dibatalkan/Pengembalian. Ditemukan juga tech debt: "Pengembalian" cuma text-parsing di catatan pelanggan, bukan model asli.
- ✅ **Keputusan user**: bangun model Return yang layak (bukan lanjutkan text-parsing). Dibuat [[T-208 Bangun model Return]] (siap diklaim, wajib desain di-approve manager dulu sebelum migrasi data — F1). T-207 sekarang depends_on T-208.
- ⏸️ User minta T-302/T-303 ditahan sampai dia kirim referensi visual (kemungkinan dari Olsera) — supaya tidak dibangun berdasar tebakan lalu dirombak ulang. Kedua task ditandai TUNGGU di board.
- 🔍 **Temuan besar**: user minta "rapikan dulu schema arsitektur" untuk Open Order Detail. Investigasi manager (baca kode langsung): (1) KOREKSI — "Dibatalkan" ternyata SUDAH punya detail sendiri (`CancelledOrderDetail.jsx`, 180 baris, rapi) — bukan berbagi dengan generik seperti dikira sebelumnya; T-303 direvisi jadi "audit & poles", bukan "bangun baru". (2) Akar masalah sesungguhnya: `Order.catatan_pelanggan` di-hack jadi penyimpanan JSON serba-guna (`metadataHelper.js` — email/alamat/kurir/dropship/staf POS/jatuh tempo, dst.), komentarnya sendiri mengaku "untuk menghindari migration". Dikonfirmasi ke `api/models.py`: field-field itu memang tidak ada di schema asli. Dibuat [[T-209 Bangun schema Order metadata]] (siap diklaim sekarang, tidak perlu tunggu referensi visual, wajib desain approved dulu — F1). T-304 (rencana cleanup React) dibatalkan — premisnya salah, `OrderDetail.jsx` sudah rapi; T-302 direvisi supaya reuse pola `CancelledOrderDetail.jsx` bukan bangun dari nol.
- 🔍 **Review manager atas desain T-209 (Antigravity): BELUM di-approve.** Antigravity benar mengikuti proses (submit desain dulu, belum ada kode diubah — dikonfirmasi `git status` bersih). Tapi 2 revisi wajib: (1) 11 field diusulkan pakai nama Inggris, padahal `Order` 100% konvensi Indonesia (U4); (2) `pos_staff` **duplikat fungsional** dari `Order.dilayani_oleh` yang sudah ada (FK ke CustomUser) — dikonfirmasi lewat `PaymentCard.jsx` kartu "Pelayan", cuma diimplementasikan ulang sebagai teks bebas tanpa integritas referensial (L4, jangan bikin field baru, sambungkan ke FK yang sudah ada). Bonus temuan: `due_date` sebaiknya dinamai persis `jatuh_tempo` — itu sekaligus memperbaiki kolom "Jatuh Tempo" di tabel `Penjualan.jsx` yang sudah merender `row.jatuh_tempo` tapi selalu kosong (field-nya belum pernah ada). Satu keputusan terbuka: `invoice_footer` per-order atau setting bisnis global — dilempar balik ke Antigravity untuk investigasi, manager yang putuskan final.
- 🆕 User minta backend: bangun router/endpoint untuk semua alur status order, diselaraskan penamaan (Indonesia). Manager cek `api/views/orders.py` + `api/urls.py` langsung: pola dedicated action yang benar sudah ada (`bayar`, `stats`, `print-return`) tapi **Selesaikan/Batalkan/Retur belum punya endpoint sendiri** — masih numpang di `PATCH /orders/:id/` generik dari 3 lokasi frontend berbeda, konfirmasi temuan sebelumnya. Dibuat [[T-210 Endpoint aksi status Order]] (siap diklaim, depends T-208 untuk endpoint retur, wajib desain approved dulu — API5). T-207 (jurnal pembalik) diupdate untuk hook di endpoint dedicated baru ini, bukan 3 lokasi PATCH lama.
- 🔍 **Review manager atas desain T-208 & T-210 (Antigravity)**: konvensi penamaan Indonesia sudah diserap dengan baik di kedua desain. T-210 Selesaikan/Batalkan **di-approve, boleh langsung implementasi** — matriks transisinya dipikirkan baik (order selesai tidak bisa dibatalkan langsung, harus lewat retur), R5 diverifikasi independen benar (`wa_logic.py` cuma baca `status_global`, tidak pernah tulis). T-208 **belum di-approve** — ketemu isu kardinalitas: `order` didesain `OneToOneField` padahal seharusnya `ForeignKey` (satu order bisa punya lebih dari satu retur sepanjang waktu, mis. retur pertama dibatalkan lalu diajukan ulang) — kalau dibiarkan OneToOne, retur kedua akan gagal karena unique constraint. Juga ketemu **konflik nyata**: T-208 mengusulkan `POST /api/pengembalian/` untuk buat retur baru, T-210 mengusulkan `POST /orders/:id/retur/` untuk hal yang sama — manager putuskan `POST /orders/:id/retur/` (T-210) yang menang, T-208 direvisi supaya `/api/pengembalian/` cuma GET (list) + PATCH (update), tidak lagi POST. T-210's bagian retur ditahan sampai T-208 resubmit dengan kardinalitas benar.
- 🔴 **T-210 (Selesaikan/Batalkan) diimplementasikan Antigravity, BELUM di-approve manager — celah otorisasi ditemukan.** 7/7 test lulus (diverifikasi ulang manager), matriks transisi & atomicity benar. Tapi endpoint baru tidak memanggil `_ensure_write_role()` (helper yang sudah ada, dipakai PATCH standar untuk menolak role `staff` mengubah status order) — akibatnya role `staff` yang seharusnya diblokir malah lolos lewat endpoint baru ini. Terbukti nyata: test yang ditulis Antigravity memakai `role="staff"` dan berhasil 200 OK tanpa disadari itu memvalidasi celahnya. Fix diminta: tambah `self._ensure_write_role()` di kedua endpoint + ganti test supaya membuktikan staff DITOLAK (403), bukan diloloskan.
- ✅ **T-210 fix diverifikasi & DI-APPROVE (Selesaikan/Batalkan).** `_ensure_write_role()` dikonfirmasi ada di kedua fungsi (`git diff`), test dipisah `kasir_user`/`staff_user` dengan benar (2 test baru: staff ditolak 403 + status benar-benar tidak berubah). 9/9 test T-210 lulus, full regression `api`+`accounting` 141 test → persis 3 kegagalan pre-existing yang sama (T-108), nol regresi baru. Sisa scope T-210 (endpoint Retur) tetap menunggu T-208 final.
- ✅ **T-208 & T-209 revisi 2 DI-APPROVE (desain).** T-208: `ForeignKey` + `related_name='daftar_pengembalian'` benar, logic tab Pengembalian eksplisit (Tunda/Dikonfirmasi masuk, Batal tidak), konflik endpoint dengan T-210 tuntas (satu pintu `POST /orders/:id/retur/`). T-209: 10 field Indonesia konsisten, `pos_staff` dibuang total (disambungkan ke `dilayani_oleh`), `jatuh_tempo` benar, `invoice_footer`/`catatan_footer` dikonfirmasi dengan bukti kode (`PaymentCard.jsx` punya textarea interaktif per-order) — bukan cuma asumsi. Keduanya **lanjut ke migration + implementasi**. T-210 menunggu migration T-208 landed untuk aktifkan Tahap 3 (endpoint retur).
- ✅ **T-208 & T-209 IMPLEMENTASI DI-APPROVE (scope masing-masing), tapi ketemu gap nyata.** Diverifikasi independen: model/migration/backfill/serializer semua benar, 10/10 test lulus, full regression 142 test bersih (3 pre-existing sama, nol baru), `PengembalianOrderViewSet` dikonfirmasi tidak punya POST (sesuai keputusan single-source). **Tapi**: `grep url_path='retur'` di `orders.py` nol hasil — endpoint `POST /orders/:id/retur/` (Tahap 3 T-210) **tidak diimplementasikan**. Dicek langsung: `ReturnOrderForm.onSave` di `Penjualan.jsx` masih 100% kode lama, retur baru terus ditulis sebagai marker teks di `catatan_pelanggan`, nol `PengembalianOrder` dibuat untuk data baru — tujuan inti T-208 (pensiunkan hack) belum tercapai untuk retur ke depan. Bukan kesalahan T-208/T-209 (itu memang scope T-210), tapi laporan implementasi menyiratkan lebih selesai dari kenyataan. T-208 & T-209 tetap **approved `done`** untuk scope masing-masing; T-210 kembali `in_progress`, Tahap 3 harus dikerjakan sebelum fitur Pengembalian benar-benar selesai end-to-end.
- 🎉 **T-210 Tahap 3 (Retur) selesai & DI-APPROVE — fondasi backend Orders TUNTAS.** `POST /orders/:id/retur/` diverifikasi lengkap: `_ensure_write_role()` ada, precondition status `selesai` benar, selalu create baru (bukan get_or_create, sesuai FK), `OrderActivityLog` tercatat, response `201`. `Penjualan.jsx` dikonfirmasi sudah pindah ke endpoint baru — jalur teks lama resmi pensiun. 5 test baru dibaca satu-satu, kualitas bagus (termasuk `test_retur_creates_multiple_per_order` yang eksplisit membuktikan kardinalitas FK dengan memanggil endpoint 2x). 15/15 test lulus, full regression 147 test → 3 pre-existing sama, nol baru, `npm run build` bersih. **T-208, T-209, T-210 semua `done`.**
- ✅ User pilih lanjut ke T-207. Sebelum dikerjakan, manager cek dulu T-201 (prasyarat, belum pernah dikerjakan) — dikerjakan sendiri (read-only, pola sama seperti T-101). **Temuan penting**: Order 100% belum terhubung ke `accounting.JournalEntry` — persis pola POS sebelum T-102/T-103 (`record_payment_to_general_ledger()` cuma tulis ke ledger legacy `hr`). Bonus temuan: `DaftarPiutang.jsx` (501 baris) ternyata **mock data statis, nol API call** — bukan risiko dual-source seperti dikira, tapi fitur yang belum pernah dibangun. **Konsekuensi: T-207 tidak bisa jalan sebelum ada jurnal asli untuk dibalik.** Urutan direvisi: dibuat [[T-202 Posting Order ke Jurnal]] (siap diklaim, mirror pola T-102/T-103, wajib desain approved dulu — F1) sebagai prioritas baru; T-207 sekarang depends_on T-202.
- 📐 [[T-209 Bangun schema Order metadata|T-209]] diklaim & usulan desain arsitektur diajukan (Antigravity): (1) 11 field eksplisit baru di `Order` (`customer_email`, `customer_address`, `shipping_courier`, `shipping_service`, `shipping_date`, `dropship_store`, `dropship_sender`, `dropship_phone`, `pos_staff`, `due_date`, `invoice_footer`); (2) command backfill idempotent `backfill_order_metadata` (`--dry-run`); (3) kontrak `OrderSerializer` DRF & update komponen UI; (4) koordinasi independen dengan T-208. Belum ada migration/kode diubah per aturan F1 — menunggu approval manager.
- 📐 [[T-208 Bangun model Return|T-208]] diklaim & usulan desain arsitektur diajukan (Antigravity): (1) Model baru `PengembalianOrder` di `api/models.py` ber-konvensi penamaan Indonesia (`order`, `tanggal_pengembalian`, `status`, `catatan`, `nominal_refund`, `dibuat_oleh`); (2) Command backfill idempotent `backfill_order_returns` (`--dry-run`); (3) Endpoint DRF `/api/pengembalian/` & integrasi UI (`Penjualan.jsx`, `ReturnOrderForm.jsx`, `ReturnOrderDetail.jsx`); (4) Pembersihan string tag `[PENGEMBALIAN - ...]`. Belum ada migration/kode diubah per aturan F1 — menunggu approval manager.
- 📐 [[T-210 Endpoint aksi status Order|T-210]] diklaim & usulan desain arsitektur diajukan (Antigravity): (1) 3 dedicated action endpoints pada `OrderViewSet` (`POST /orders/:id/selesaikan/`, `POST /orders/:id/batalkan/` dengan payload `alasan`, `POST /orders/:id/retur/` terkait model `PengembalianOrder` T-208); (2) Matriks validasi transisi status; (3) Audit R5 mengonfirmasi `wa_logic.py` tidak menyentuh `status_global` via PATCH, generic `PATCH` tetap difungsikan; (4) Eksekusi Selesaikan & Batalkan didahulukan, Retur diaktifkan setelah migration T-208. Belum ada kode diubah per aturan API5 — menunggu approval manager.
- 🔄 [[T-210 Endpoint aksi status Order|T-210]] Selesaikan & Batalkan diimplementasikan & diajukan untuk `review` (Antigravity): (1) Dedicated action `@action` `POST /orders/:id/selesaikan/` dan `POST /orders/:id/batalkan/` di `OrderViewSet` (`api/views/orders.py`); (2) Matriks validasi transisi status mencegah order selesai dibatalkan/order batal diselesaikan; (3) 7/7 unit test di `api/tests_order_status_actions.py` PASSED (OK); (4) Integration frontend `OrderDetail.jsx` dipindahkan ke endpoint baru; (5) Frontend build `npm run build` sukses (2.53s, 0 error); (6) `graphify update .` tuntas. Status diset ke `review`.
- 🔒 [[T-210 Endpoint aksi status Order|T-210]] Celah otorisasi diperbaiki & diajukan ulang untuk `review` (Antigravity): (1) `self._ensure_write_role()` ditambahkan di baris pertama `selesaikan()` dan `batalkan()` pada `api/views/orders.py`; (2) Role `staff` terbukti ditolak (403 Forbidden) lewat 2 unit test baru `test_selesaikan_denied_for_staff` dan `test_batalkan_denied_for_staff`; (3) Setup test sukses disesuaikan ke role `kasir` (9/9 test PASSED); (4) Full test suite dijalankan (0 regresi baru); (5) `graphify update .` tuntas. Status diset ke `review`.
- 📐 [[T-208 Bangun model Return|T-208]] Revisi 2 usulan desain diajukan (Antigravity): (1) Field `order` diubah ke `ForeignKey` (`related_name='daftar_pengembalian'`) untuk mendukung histori multiple retur; (2) `POST /api/pengembalian/` dihapus dari desain, pembuatan retur disatukan ke single-source endpoint `POST /orders/:id/retur/` (T-210); (3) `/api/pengembalian/` khusus `GET` (list) & `PATCH` (update); (4) `OrderSerializer` menyediakan `pengembalian_aktif` (retur bukan Batal) dan `daftar_pengembalian`; (5) Tab Pengembalian `Penjualan.jsx` hanya memuat order dengan retur aktif (`Draft`/`Tunda`/`Dikonfirmasi`). Belum ada migration/kode diubah per aturan F1 — menunggu approval manager.
- 📐 [[T-209 Bangun schema Order metadata|T-209]] Revisi 2 usulan desain diajukan (Antigravity): (1) Seluruh 10 kolom baru dinamai dalam Bahasa Indonesia (`email_pelanggan`, `alamat_pelanggan`, `kurir_pengiriman`, `layanan_pengiriman`, `tanggal_pengiriman`, `toko_dropship`, `pengirim_dropship`, `telepon_dropship`, `jatuh_tempo`, `catatan_footer`); (2) Field `pos_staff` dihapus dari desain, kartu Pelayan POS di `PaymentCard.jsx` disambungkan langsung ke FK `Order.dilayani_oleh` (`CustomUser`); (3) Field `due_date` dinamai persis `jatuh_tempo`, memperbaiki kolom Jatuh Tempo di `Penjualan.jsx`; (4) Investigasi `invoice_footer` mengonfirmasi UI `PaymentCard.jsx` menyediakan editor per-order via `<textarea>`, sehingga `catatan_footer` dipertahankan per-order. Belum ada migration/kode diubah per aturan F1 — menunggu approval manager.
- 🔄 [[T-208 Bangun model Return|T-208]] Implemented & Submitted for `review` (Antigravity): (1) Model `PengembalianOrder` + Migration `0088` landed (`order = ForeignKey`); (2) Management command `backfill_order_returns` (`--dry-run`) tuntas; (3) Endpoint `/api/pengembalian/` (`GET` list/detail & `PATCH` update) & nested fields `pengembalian_aktif`/`daftar_pengembalian` di DRF; (4) Frontend `ReturnOrderDetail.jsx` & `Penjualan.jsx` terintegrasi; (5) Frontend build `npm run build` (1.98s, 0 error) & unit test (10/10 PASS); (6) `graphify update .` tuntas. Status diset ke `review`.
- 🔄 [[T-209 Bangun schema Order metadata|T-209]] Implemented & Submitted for `review` (Antigravity): (1) 10 kolom Bahasa Indonesia pada model `Order` + Migration `0088` landed; (2) `pos_staff` dihapus & disambungkan ke `Order.dilayani_oleh`; (3) Field `jatuh_tempo` mengisikan kolom Jatuh Tempo tabel `Penjualan.jsx`; (4) Management command `backfill_order_metadata` (`--dry-run`) tuntas; (5) Frontend `metadataHelper.js`, `OrderDetail.jsx` & `PaymentCard.jsx` terintegrasi ke field DB asli; (6) Frontend build `npm run build` (1.98s, 0 error) & unit test (10/10 PASS); (7) `graphify update .` tuntas. Status diset ke `review`.
- 🔄 [[T-210 Endpoint aksi status Order|T-210]] Tahap 3 (Retur) Implemented & Submitted for `review` (Antigravity): (1) `POST /orders/:id/retur/` ditambahkan ke `OrderViewSet` — `_ensure_write_role()`, precondition `status_global == 'selesai'`, selalu `PengembalianOrder.objects.create()` baru (bukan get_or_create), catat `OrderActivityLog tindakan='RETURN'`; (2) `ReturnOrderForm.onSave` di `Penjualan.jsx` dipindahkan dari PATCH marker lama ke `POST /orders/:id/retur/`; (3) 5 test baru via endpoint API (15/15 PASS); (4) `npm run build` sukses (2.05s, 0 error). Status diset ke `review`.
- ✅ **Desain [[T-202 Posting Order ke Jurnal|T-202]] DI-APPROVE manager** setelah verifikasi independen ke kode asli (bukan cuma percaya laporan): `JournalEntry.source_id`/`idx_je_source`, `OrderActivityLog` PK auto-increment + `tindakan='PAYMENT'` di posisi tepat sebelum `record_payment_to_general_ledger()`, pola `AccountingSettings.pos_sales_revenue_account`, signature `create_journal_entry()` — semua klaim akurat. 6/7 poin desain (momen posting per-`bayar()`, idempotency via `OrderActivityLog.id`, cash-basis piutang v1, gating fail-open, service `order_posting.py`, scope T-207/PPN/HPP di luar task ini) disetujui tanpa revisi. Satu keputusan diminta ke user (X6): akun pendapatan Order (`order_sales_revenue_account`) dipisah atau disamakan dengan POS — **user memutuskan dipisah**, dengan syarat `help_text` yang jelas membedakan dari `pos_sales_revenue_account` supaya admin tidak bingung di UI Pengaturan Akuntansi. Antigravity boleh lanjut implementasi.
- 🔴 **[[T-202 Posting Order ke Jurnal|T-202]] diimplementasikan Antigravity, submit untuk `review` — DITOLAK manager, dikembalikan `in_progress`.** Test diulang manager: 160 test, 157 pass, 2 fail + 1 error — cocok dengan klaim & terkonfirmasi sama seperti kegagalan pre-existing T-108, nol regresi. Tapi ketemu gap blocking: **tidak ada kode yang me-resolve `Order.accounting_payment_method` dari string `metode_pembayaran`** — beda dengan POS yang punya resolver di `api/pos_services.py:319-334`. Akibatnya `should_post_order_payment()` akan selalu skip (field selalu `None`) untuk SEMUA pembayaran Order di dunia nyata — dikonfirmasi langsung dari log test (`"belum dipetakan ke accounting.PaymentMethod"` muncul saat test "happy path" jalan). Test tidak menangkap ini karena fixture set `accounting_payment_method` langsung via ORM, bukan lewat alur `bayar()`/`perform_create()` sesungguhnya — pola bug yang sama seperti T-210's authorization gap (test memvalidasi hal yang salah). Bonus temuan minor: `perform_create()` (jalur DP awal) tidak dibungkus `transaction.atomic` (pre-existing, tapi sekarang menampung 2 tulisan uang bukan 1). Fix diminta: tambah logic resolusi meniru `pos_services.py`, tambah test yang tidak pre-set FK, pertimbangkan atomic wrapper di `perform_create`. Detail lengkap di [[T-202 Posting Order ke Jurnal|T-202]] §Review Manager.
- 🔄 **[[T-202 Posting Order ke Jurnal|T-202]] IMPLEMENTASI selesai, diajukan untuk `review` manager (Antigravity):** (1) `accounting/migrations/0021` — `AccountingSettings.order_sales_revenue_account` (FK, `help_text` pembeda) + `JournalEntry.SourceType.ORDER_PAYMENT` choice baru; (2) Service baru `accounting/services/order_posting.py` — `should_post_order_payment()` (5 gating checks) + `post_order_payment_journal()` idempotent via `create_journal_entry()` (M2), retry 3x; (3) `api/views/orders.py` — lazy import + call `post_order_payment_journal()` di `bayar()` dan `perform_create()`, paralel dengan legacy `record_payment_to_general_ledger()` (R2, tidak dihapus); (4) `accounting/tests_order_posting.py` — 13 test (9 unit + 4 API integrasi): happy path DP/pelunasan, idempotency, 4 gating scenarios. 13/13 test T-202 PASS, full regression 157 pass, 3 failures **dikonfirmasi pre-existing** (git stash verify identik sebelum T-202), `npm run build` sukses, `graphify update .` tuntas (4215 nodes, 12570 edges). Status diset ke `review`.
